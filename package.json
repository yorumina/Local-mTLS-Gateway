{
  "name": "opencode-mtls-sidecar",
  "version": "0.1.0",
  "private": true,
  "description": "Loopback-only OpenAI-compatible proxy with an outbound mTLS client certificate.",
  "type": "module",
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "start": "node src/server.mjs",
    "check": "node scripts/policy-check.mjs",
    "smoke": "node scripts/smoke-test.mjs"
  }
}

